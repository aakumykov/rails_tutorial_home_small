<%= form_for(current_user.relationships.build(author_id: @user.id), remote:true) do |f| %>
  <div><%= f.hidden_field :author_id %></div>
  <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>